name: Branch Selection Workflow

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to perform operation on'
        required: true

jobs:
  select-branch:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: List branches
        run: |
          branches=$(git for-each-ref --format '%(refname:short)' refs/heads/)
          echo "Available branches: $branches"
          echo "::set-output name=branches::$branches"

      - name: Select branch
        id: select-branch
        run: echo "Selected branch: ${{ github.event.inputs.branch }}"

      - name: Perform operation on selected branch
        run: |
          # Your operations on the selected branch go here
          # For example: git checkout ${{ steps.select-branch.outputs.branch }}
